"""Integration tests for GPT-Trader.

These tests validate end-to-end functionality using real component wiring
(but mock broker) rather than mocked dependencies.

Run with: pytest tests/integration -m integration
"""
