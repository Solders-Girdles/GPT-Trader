# Archived Scripts

ARCHIVED: These scripts are not referenced by docs/CI/Makefile and were moved to
reduce the active repo surface area. They are kept for historical reference.

If you need to revive one, move it back under `scripts/` and document the usage.
