# <Decision title — a short noun phrase>

---
status: proposed
date: <YYYY-MM-DD>
deciders: <owner>
supersedes:
superseded-by:
---

> Copy this file to `docs/decisions/<slug>.md`. While `status: proposed`, this is
> an open decision the owner has not yet made. See the
> [decisions README](README.md) for the lifecycle.

## Context

What forces a choice now? Link the evidence: code paths, issues, prior decisions.
Keep it to what a decider needs — not a full audit.

## Options

- **Option A — <name>.** What it means, and the trade-off.
- **Option B — <name>.** What it means, and the trade-off.

## Decision

State the chosen option in one or two sentences. Fill this in when `status`
becomes `accepted`. Leave the rejected options visible above so they are not
relitigated.

## Consequences

What changes as a result — code, config, follow-up work, new constraints. File
the follow-up work as GitHub issues and link them; do not list a backlog here.

## Safety boundary

For trading-system decisions: state explicitly whether this authorizes any
broker/API call, live execution, money movement, or autonomy change. Default is
that it does not.
