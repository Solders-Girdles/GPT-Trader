# Environment Templates

- Local development: copy `config/environments/.env.template` to `.env` (or `.env.local`).
- Production: start from the same template and create `config/environments/.env.production` locally (do not commit it).
