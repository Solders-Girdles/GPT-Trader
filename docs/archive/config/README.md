# Archived Configuration Files

These YAML files were retained as historical references from the pre-DI era.
They are not loaded by the current runtime or tooling. Prefer the active
configuration surfaces under `config/` (profiles, risk, intelligence, and
environment templates).
